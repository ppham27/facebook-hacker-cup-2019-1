// swift-tools-version:5.1
// The swift-tools-version declares the minimum version of Swift required to build this package.

import PackageDescription

let package = Package(
  name: "2019_1",
  dependencies: [],
  targets: [
    .target(
      name: "GraphsAsAService",
      dependencies: []),
    .target(
      name: "ClassTreasurer",
      dependencies: []),
  ]
)
